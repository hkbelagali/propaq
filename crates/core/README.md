## propaq-core 

This crate implements the core data structures and algorithms for propagation, including the 
traits for algebraic structures, string representation, and generic coefficient types. **This 
crate is not intended for direct use by end users.** It is a dependency of the `propaq` software, 
and is best utilized through the Python interface. 
