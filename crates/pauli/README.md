## propaq-pauli 
This crate implements the Pauli algebra and thin wrappers around the generic propagation engine. **This crate is not intended for direct use by end users.** It's best accessed using the 
Python interface in `propaq`.
