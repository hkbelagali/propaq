## propaq-surrogate
This crate provides symbolic coefficient structures and wrappers around the generic engines for surrogate propagation and evaluation. **This is not intended to be used directly by end users.** It's best used 
through the Python interface of propaq.